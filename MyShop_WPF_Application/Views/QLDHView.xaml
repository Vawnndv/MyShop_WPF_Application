<Window x:Class="MyShop_WPF_Application.Views.QLDHView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyShop_WPF_Application.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="QLDHView" Height="450" Width="800"
        Loaded="Window_Loaded"
        >

    <ListView x:Name="lst" HorizontalAlignment="Left" VerticalAlignment="Top" Width="1000" d:ItemsSource="{d:SampleData ItemCount=5}">

        <ListView.View >
            <GridView>
                <GridViewColumn Header="ID" Width="100" DisplayMemberBinding="{Binding ID}" />
                <GridViewColumn Header="Trạng thái" Width="150" DisplayMemberBinding="{Binding status}"/>
                <GridViewColumn Header="Ngày tạo" Width="150" DisplayMemberBinding="{Binding createDate}"/>
                <GridViewColumn Header="Số điện thoại" Width="150" DisplayMemberBinding="{Binding phoneNumber}"/>
                <GridViewColumn Header="Tổng giá trị" Width="200" DisplayMemberBinding="{Binding totalValue}"/>
            </GridView>
        </ListView.View>
        
       
        
      
    </ListView>
</Window>
